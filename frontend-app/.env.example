# Copy to .env.local and set your production API origin (no trailing slash).
# Dev: leave empty — Vite proxies /api to the backend in vite.config.js
# VITE_API_BASE=https://dupefinder-api.up.railway.app
