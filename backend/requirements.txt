# DupeFinder Backend Dependencies
# Updated: November 11, 2025 - Added authentication dependencies and MongoDB Atlas support

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database
sqlalchemy==2.0.23
# psycopg2-binary: optional (no wheel for Python 3.14 yet; project uses MongoDB)
pymongo>=4.6.0
motor>=3.3.2  # Async MongoDB driver for Atlas
alembic>=1.13.0

# Environment & Configuration
python-dotenv>=1.0.0

# Authentication & Security
PyJWT>=2.8.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<4.1  # 4.1+ breaks passlib (no __about__)

# Email (for OTP)
aiosmtplib>=3.0.0
email-validator>=2.1.0

# Image Processing (for handling uploads)
Pillow>=10.1.0

# ML & AI (for embedding extraction; search route loads ml-engine)
torch>=2.2.0
transformers>=4.35.0
numpy>=1.26.0
scipy>=1.10.0
tqdm>=4.65.0
faiss-cpu>=1.7.0  # similarity search (import faiss)

# API & Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP Requests
httpx>=0.25.0
aiohttp>=3.9.0

# Web Scraping (Playwright for JS-rendered pages; BeautifulSoup for parsing)
playwright>=1.40.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Excel File Reading
openpyxl>=3.1.0
pandas>=2.1.0

# Utilities
python-slugify>=8.0.0

# CORS
fastapi-cors>=0.0.6
