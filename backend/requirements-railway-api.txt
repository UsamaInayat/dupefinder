# DupeFinder Backend Dependencies (Railway API image)
#
# This file is like `requirements.txt`, but intentionally omits Playwright because
# browser automation is split into `Dockerfile.railway-scraper`.

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database
sqlalchemy==2.0.23
pymongo>=4.6.0
motor>=3.3.2
alembic>=1.13.0

# Environment & Configuration
python-dotenv>=1.0.0

# Authentication & Security
PyJWT>=2.8.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<4.1

# Email (for OTP)
aiosmtplib>=3.0.0
email-validator>=2.1.0

# Image Processing (for handling uploads)
Pillow>=10.1.0

# ML & AI (for embedding extraction; search route loads ml-engine)
# torch: installed in Dockerfile.railway-api from PyTorch *CPU* wheel index (much smaller than default CUDA wheels).
transformers>=4.35.0
# NumPy 2.x breaks some wheels/extensions built for 1.x (FAISS/torch edge cases). Stay on 1.26+ until everything is 2.0-native.
numpy>=1.26.0,<2.0.0
tqdm>=4.65.0
faiss-cpu>=1.7.0

# API & Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP Requests
httpx>=0.25.0
aiohttp>=3.9.0

# HTML parsing helpers (Playwright itself is not installed in this image)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Excel File Reading
openpyxl>=3.1.0
pandas>=2.1.0

# Utilities
python-slugify>=8.0.0

# CORS: use FastAPI's built-in CORSMiddleware (see app/main.py) — avoid extra package.
